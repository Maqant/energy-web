/*
 * MenuSection Messages
 *
 * This contains all the text for the MenuSection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuSection component!',
  },
});
